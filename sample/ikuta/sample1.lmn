//--wtモードで実行してみてください。

{
	window.
	name("test").
	size(300, 300).
	sys.perpetual(on).

	{button, position(0,0), label("label1")}.
	
	{button, position(1,0)}.
	
	{textarea, position(0,1), weight(1.0, 1.0)}.
	
	{textarea, position(1,1), text("aaaaaaaaaaa"), weight(1.0, 1.0)}.
	
	
}
