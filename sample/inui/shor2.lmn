/*
 * ショアのアルゴリズム（量子コンピュータによる素因数分解）
 * レジスタを膜で表現
 * (作成中)
 */
{
r1.
x(integer.set(1,4)).
}.

{r2}.

{r1, x(X), $p, @p}, {r2, $q, @q} :- int(X) | {r1, x(X, Y), $p, @p}, {r2, Y=fNx(integer.powermod(11, X, 15)), $q, @q}.

//観測したら１つの値に収束
{r2, fNx(Y, x(X)), fNx(Y2, x(X2)), $p, @p} :- Y > Y2 | {r2, fNx(Y, X), $p, @p}, nlmem.kill(X2).
