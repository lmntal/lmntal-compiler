/**
NAME
	Object-Oriented Programming List

SYNOPSIS
	オブジェクトを格納するリスト

AUTHOR
	Atsuyuki Inui

HISTORY
	2006/10/03(Tue) 
 */
{
module(ooplist).

H=ooplist.new(N) :- N = 0 | H=[].
H=ooplist.new(N) :- N > 0 | H=[{}|ooplist.new(N-1)].

H=ooplist.get([{$p,@p}|Tail], N, Ref) :- N=0 | H=[{$p,@p,+Ref}|Tail].
H=ooplist.get([Head|Tail], N, Ref) :- N>0 | H=[Head|ooplist.get(Tail,N-1,Ref)].

}.