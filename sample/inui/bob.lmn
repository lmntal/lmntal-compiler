//bob.lmnに続いてalice.lmnを実行する
java_net_Socket.use.

server=accept(java_net_ServerSocket.new(10000), Socket).
socket=getInputStream(Socket, IS).
br=readLine(java_io_BufferedReader.new(java_io_InputStreamReader.new(IS)), s).
