/* 
 * オブジェクトを膜で表現
 * オブジェクトをコピーしないで受け取る
 */

//LMNtalのオブジェクトとJavaのオブジェクトの両方を受け取れる
{
module(line).
H=line.new(Start, End) :-
	H={start(Start), end(End)}.
H=line.new(X1, Y1, X2, Y2) :-
	H={start(Start), end(End)},
	Start=point.new(X1, Y1), End=point.new(X2, Y2).
H=length({start(Start), end(End)}, Length) :- H={
	start=Start2.
	end=End2.
	},
	Start2=getY(getX(Start, X1), Y1),
	End2=getY(getX(End, X2), Y2),
	Length=math.sqrt((X1-X2)*(Y1-Y2)).
}.
