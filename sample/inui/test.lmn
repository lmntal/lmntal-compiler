//start :- runnable.
//runnable :- run.
//run :- runnable.
//run :- wait_for_run.
//run :- end.
//wait_for_run :- runnable.
//
//start.

//{displays, $p}, t(T), t1(T1) :- T = T1 | {alarms_beep, alarm1_beep, $p}, t(T), t1(T1).
//{displays, $p}, t(T), t2(T2) :- T = T2 | {alarms_beep, alarm2_beep, $p}, t(T), t2(T2).
//
//{alarms_beep, a, $p} :- {displays, $p}.
//{alarms_beep, b, $p} :- {displays, $p}.
//{alarms_beep, c, $p} :- {displays, $p}.
//{alarms_beep, d, $p} :- {displays, $p}.
//
//{displays, time, d} :- {displays, date}.
//{displays, date, d} :- {displays, time}.
//{displays, time, a} :- {displays, alarm1}.
//{displays, alarm1, a} :- {displays, alarm2}.
//{displays, alarm2, a} :- {displays, chime}.
//{displays, chime, a} :- {displays, stopwatch}.
//{displays, stopwatch} :- {displays, time}.

start.

start :- open.

open, all :- strike.
open, rest :- before_2.

before_2, rest :- open.
before_2, all :- spair.

spair, rest :- before_2.
spair, all :- strike.

strike, rest :- after_s_before_2.
strike, all :- double.

double, rest :- after_s_before_2.
double, all :- double.

after_s_before_2, all :- spair.
after_s_before_2, rest :- open.
