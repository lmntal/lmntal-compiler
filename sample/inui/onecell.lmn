{
module(oneCell).
H=oneCell.new :- H=empty.
put@@
H=put(empty, Object) :- H=contains(Object).
get@@
H=get(contains(Object), Res) :- H=empty, Res=Object.
}.

b=oneCell.new.

n(0).
n(N), b(B) :- N < 2 | b=put(B, N), n(N+1).

m(0), a=[].
m(M), a(A), b(B) :- M < 2 | b=get(B, X), m(M+1), a=[X|A].
