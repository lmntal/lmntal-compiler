H=intList(M,N) :- M>N  | H=[].
H=intList(M,N) :- M=<N | H=[M|intList(M+1,N)].

product(intList(1,3),intList(1,3)).

  product([],M) :- ground(M) | .
  product([L|Ls],M) :- ground(M) | p2(L,M), product(Ls,M).

  p2(L,[]) :- unary(L) | .
  p2(L,[M|Ms]) :- unary(L) | pair(L,M), p2(L,Ms).
