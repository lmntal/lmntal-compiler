X0=[$opL,exp(E1),$op,exp(E2),$opR|X],
op($opL,PL), op($op,P), op($opR,PR) :-
    unary($opL), unary($op), unary($opR), PL=<P, P>=PR |
    X0=[$opL,exp(e($op,E1,E2)),$opR|X],
    op($opL,PL), op($op,P), op($opR,PR).

X0=[N|X] :- int(N) | X0=[exp(N)|X].

ans1([begin,123,'+',45,'*',67,'+',8,end]).
// ans2([begin,123,'*',45,'+',67,'*',8,end]).
op('*',4).  op('*',4).  op('*',4).
op('+',3).  op('+',3).  op('+',3).
op(begin,0).  op(end,0).
