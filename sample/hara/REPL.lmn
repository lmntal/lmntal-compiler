/**
 *  Distributed LMNtal sample(#1)
 *  distributed sort
 *    クライアントがサーバからソートプログラムをダウンロードして実行し，
 *    その結果をサーバがマージする．
 *  server file: sort-server.lmn
 *
 *  06/01/12 okabe
 */

io.use.

r=run(io.list_of_stdin_async).

Runtime_Run@@
R=run([H|T]), runtime_dumped={$p[], @p}/ :- R=run(T), runtime_run={decoder.stringDecoder(H)}.

Dump@@
runtime_run={$p[], @r} :- runtime_run={$p[], @r, sys.dump}.
