
{
 list = gen_list(750).
 L = gen_list(0) :- L = [].
 L = gen_list($i) :- $i > 0, $ii = $i - 1 | L = [$i | gen_list($ii)].
  }.
  
{ $p, @p }/ :- $p.

L = [ $p, $q | R] :- $p > $q | L = [$q, $p | R].
