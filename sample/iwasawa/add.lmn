t0_init{
  add(X, Y, s(Z)) :- add(X, s(Y), Z). 
  add(X, Y, s(Z)) :- add(s(X), Y, Z).
  
  
  add(0, 0, s(s(s(0)))).
}.

/*
t0_init{
  add(X, 0, Z) :- x = X, y = Z.
  add(X, Y, s(Z)) :- add(s(X), Y, Z).
   
  add(0, 0, s(s(s(0)))).
}.
*/

t0_init{$p, @p} :- t0_init{$p, @p}.
t0_init{add($x, $y, 0), $p, @p} :-
 ground($x), ground($y) |
 accept_all_end{add($x, $y, 0), $p, @p}.
 