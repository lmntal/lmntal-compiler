{
	{$p} :- $p.
	thread.create(4).
	thread.all({norm(0)}_).
	thread.all({norm(Y), n(X) :- int(X),Z=X*X+Y | norm(Z)}_).
	generate(1000).
	generate(X) :- X>0 | thread.at(X mod 4, {n(integer.rnd(100))}_), generate(X-1).
	norm(N), norm(M) :- norm(N+M).
}