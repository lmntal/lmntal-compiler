{
	thread.create(4).
	thread.all({norm(0)}).
	thread.all({generate(2500)}).
	thread.all({gen@@ generate(X) :- X>0,Y=X-1 | n(integer.rnd(100)), generate(X-1)}).
	go@@
		go :- thread.all({cal@@ norm(Y), n(X) :- int(X),Z=X*X+Y | norm(Z)}).
	norm@@
		norm(N), norm(M) :- norm(N+M).
}.