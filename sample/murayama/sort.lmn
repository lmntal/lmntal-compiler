timer:benchmark(X,"Elapsed Time"),
{
	+(X).
	length=io.inputInteger(mes).
	ret=[].
	start(0).
	start(S), length(L), ret=L2 :- S<L | start(S+1), length(L), ret=[S|L2].
	{L=[X,Y|L2] :- X>Y | L=[Y,X|L2].}.
	go, {@p} :- @p.
}.