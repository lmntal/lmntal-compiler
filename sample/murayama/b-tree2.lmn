% B木の根
root({r=[6],num(1)}). 

% rootに次々と値を代入する
create([9,15,20,81]).
create([]) :-.
root({R=[H1|B1],num(N),$p[R,B1|*Z]}), create([H2|B2])
    :- int(N),int(H1) | root({R=[H2,H1 | B1],num(N+1),$p[R,B1|*Z]}), create(B2). 
    
% レコード内をsort（未完成）
{R=[H1, H2 | B],$p[R,B|*Z]}
	:- H1<H2 | {R=[H2, H1 | B],$p[R,B|*Z]}.

% レコードの分割
{r=[H1,H2,H3 | B],num(5),$p[B|*Z]}
	:-
	| {r=[+(T1),H3,+(T2)],num(1)},
	  {+(T1),r=[H1,H2],num(2)},
	  {+(T2),r(B),num(2),$p[B|*Z]}.