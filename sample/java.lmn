/*
NAME
	java のオブジェクトに透過的にアクセスするサンプル

SYNOPSIS

AUTHOR
	Koji Hara

HISTORY
	2004/07/15(Thu)

*/
sys.perpetual(on).

r0=java.new("java.awt.Frame", ["test"]).

r0=H :- unary(H) | r1=H, s=java.invoke(H, show).
r1=H :- unary(H) | r2=H, s=java.invoke(H, setSize, [200, 300]).

