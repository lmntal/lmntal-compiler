/*
empty(1, 1, Y1, W1, "", ""),     empty(2, 1, Y2, X1, "", W1),     empty(3, 1, Y3, "", "", X1),     
empty(1, 2, Z1, W2, Y1, ""),     black(2, 2, Z2, X2, Y2, W2),     white(3, 2, Z3, "", Y3, X2),
empty(1, 3, "", W3, Z1, ""),     white(2, 3, "", X3, Z2, W3),     black(3, 3, "", "", Z3, X3).

x(1), y(2).


white(X, Y, Z, D, E, F), x(G), y(H) :- X=G-1, Y=H-1 | black(X, Y, Z, D, E, F).
*/

/*
empty(X, Y, Z, U, V, W) :- X=1, Y=1 | black(X, Y, Z, U, V, W).
*/

/*
black(1, 1, A), white(2, 2, A).
black(A, B, C), white(D, E, F) :- A>0, B>0, D>0, E>0, A=B | black(A, B, C), black(D, E, F)
*/

/*
black(1, 1, A), white(2, 2, A), check, x(1), y(1).
black(U, V, W), white(X, Y, Z) :- U=1, V=1 | black(U, V, W), black(X, Y, Z).
*/

/*
black(1, 1, A), white(2, 2, A), check, x(1), y(1).
black(X, Y, Z), white(U, V, W) :-  black(X, Y, Z), black(U, V, W).
*/

/*
a(1, 1, 2), {a(2, 2, 3), a(3, 3, 4)}.
a(A, B, C) :- A=1 | b(A, B, C).
{a(A, B, C) :- A=2 | c(A, B, C)}.
*/

/*
m(2), n(1), loop.
m(X), n(Y), loop :- X>Y | m(X+1), n(Y+1).
*/
/*
a(1, 2), b(2, 3).
a(A, B), b(C, D) :- a(A, B), b(C, D), c(A, X), d(B, X).
*/

/* -------------------------------------------------------------------- */

/*
m(1), n(1), get(0).

/* 探索上限値 */
m(M), n(N), get(G) :- M>500, N>0, G>=0 |.

/* 各数の検査の開始位置 */
m(M), n(N), get(G) :- M=<N, M=\=G | m(M+1), n(1), get(0).
m(M), n(N), get(G) :- M=<N, M=G | m(M+1), n(1), get(0), perfect(G).

/* 約数を見つけたら加算 */
m(M), n(N), get(G) :- M>N, mod(M,N)=0 | m(M), n(N+1), get(N+G).
m(M), n(N), get(G) :- M>N, mod(M,N)>0 | m(M), n(N+1), get(G).

/* perfectが見つかったときの開始位置 */
//m(M), n(N), get(G) :- M>N, M=G | m(M+1), n(1), get(0), perfect(G).
//m(M), n(N), get(G) :- M>N, M<G | m(M+1), n(1), get(0).

*/