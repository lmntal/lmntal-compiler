{
	io.print(call(m, m.incr)).
}.
{
	io.print(call(m, m.incr)).
}.
m : {
	static.load.
	name(m).
	m.incr(ready).
	id(0).
	( m.incr(msg(X)), id($o) :- int($o),$n=$o+1 | cp($n, C1, C2), id(C1), X=C2, m.incr(ready) ).
}.

