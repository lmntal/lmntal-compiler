% mutex.lmn
%     2009.05.26
%
% Maude2LMNtal

'$'.
a(wait), b(wait).

a_enter @@ a(wait), '$' :- a(crit) .
b_enter @@ b(wait), '*' :- b(crit) .
a_exit  @@ a(crit) :- a(wait), '*' .
b_exit  @@ b(crit) :- b(wait), '$' .

% property0: []!(crit(a)&&crit(b))
% prpoerty1: ([]<>wait(a)) -> ([]<>crit(a))
% property2: []wait(b)