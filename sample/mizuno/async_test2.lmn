{
	{
		{
			gen.
			gen@@gen :- {a.
						 in@@a :- b,[:/*inline*/try{Thread.sleep(20);mem.removeAtom(me);}catch(Exception e){}:].
						}@"localhost".
			out@@{b,$p,@p} :- {a,$p,@p}.
		}.
	}.
	delete(100).
	delete@@delete(N), {{$p,@p}} :- N>0, N2=N-1 | insert(N2), {$p,@p}.
	insert@@insert(N), {$p,@p} :- N>0, N2=N-1 | delete(N2), {{$p,@p}}.
}.

