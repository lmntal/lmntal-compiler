[[/*inline*/System.out.println(me.nth(0));]](test)

a,a,a,(a:-{b(X)},c(X)),({b(X)},c(X):- d(E),{e(E)})

a(X),{g(X)},(a(X),{g(X)}:- e(X),{h(X)})

f(X),{g(X)}

append(c(1,c(2,c(3,n))),c(4,c(5,n)),result), ( append(X,Y,Z), n(X) :- Y=Z ), ( append(X,Y,Z), c(A,X1,X) :- c(A,Z1,Z), append(X1,Y,Z1) )

(a($p) :- int($p) | b($p))

(a(X) :- int(X) | b(X))

p([1,2,3])

test((123+45-67+89)/(10*10))

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% プロセス文脈のテスト

1,1,1,1,1,{},(1,{$p}:-{$p,1}) % ==> {1,1,1,1,1}
% RemoveProxies のテスト
{{a(X)},b(X,Y)},{c(Y)}, ({$p},{$q} :- {$p,$q}) % ==> {{a(X)},b(X,Y),c(Y)}
% InsertProxies のテスト
{{a(A,X)},b(B,X,C)},c(A,B,C), ({{$p},$q} :- {$p,$q}) % ==> エラー

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% システムルールセットのテスト

% 外に出すシステムルールのテスト
{p(X,Y), (p(X,Y) :- X=Y)}, a(X,Y) % ==> a(X,X)

% 中に入れるシステムルールのテスト
{a(X,Y)}, p(X,Y), (p(X,Y) :- X=Y) % ==> {a(X,X)}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
