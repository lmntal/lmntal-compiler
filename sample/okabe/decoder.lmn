/*
 *  sample: lib/src/decoder.lmn
 */

%decoder.stringDecoder("a. a:-b. b:-c").
%decoder.fileDecoder("wtCalc.lmn").

{a(X),b(Y).
[:/*inline*/
  me.remove();
  System.out.println(mem.encode());
:].
}.

% 送られてきた自由リンクを繋ぎ直す方法
% 上の膜が{b(_13), a(_15)}とencodeされたと仮定する
% この方法だと自由リンクのリンク名は受信側にあらかじめ知らされてなければならない
decoder.stringDecoder
  (string.concat("ret=someProcess(_13,_15),","{b(_13), a(_15)}")).