% dlt.close, dlt.terminate のテスト

dlt.bind(50000,[accept(times(X,3),X),close]).

% timesフィルタ
Ret=times([H|T],N) :- int(N),string(H) |
  Ret=[string.times(H,N)|times(T,N)].

% close -> terminate
closed(S,R) :- terminated(S,R),dlt.terminate.