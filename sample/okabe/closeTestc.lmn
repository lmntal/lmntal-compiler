% dlt.close, dlt.terminate のテスト

dlt.connect("127.0.0.1",50000,["I","am","hoge"],ret).

% データを１つでも受け取ったらclose
dlt.socket(Send,Recv,Obj),Ret=[H|Recv] :-
  dlt.socket([close|Send],Recv,Obj),Ret=[H|Recv].

% close -> terminate
closed(S,R) :- terminated(S,R),dlt.terminate.