/* The following code was generated by JFlex 1.4_5 on 06/07/07 23:40 */

/**
	This is LMNtal Lex file.
*/

/* --------------------------Usercode Section------------------------ */
package compile.parser;
import java_cup.runtime.Symbol;


/**
 * This class is a scanner generated by 
 * <a href="http://www.jflex.de/">JFlex</a> 1.4_5
 * on 06/07/07 23:40 from the specification file
 * <tt>lmntal.flex</tt>
 */
class Lexer implements java_cup.runtime.Scanner {

  /** This character denotes the end of file */
  public static final int YYEOF = -1;

  /** initial size of the lookahead buffer */
  private static final int YY_BUFFERSIZE = 16384;

  /** lexical states */
  public static final int STRING = 2;
  public static final int QUOTED = 1;
  public static final int YYINITIAL = 0;
  public static final int COMMENT = 3;

  /** 
   * Translates characters to character classes
   */
  private static final String yycmap_packed = 
    "\11\0\1\12\1\2\1\0\1\3\1\1\22\0\1\12\1\26\1\23"+
    "\1\22\1\52\1\35\1\0\1\24\1\40\1\41\1\36\1\17\1\37"+
    "\1\46\1\15\1\34\1\20\11\14\1\30\1\0\1\32\1\25\1\33"+
    "\1\31\1\44\2\4\1\4\1\4\1\16\1\4\24\13\1\53\1\27"+
    "\1\54\1\50\1\45\1\0\1\6\1\6\1\6\1\57\1\11\1\61"+
    "\1\5\1\5\4\5\1\55\1\60\1\56\2\5\1\7\1\5\1\10"+
    "\3\5\1\21\2\5\1\42\1\47\1\43\1\51\uff81\0";

  /** 
   * Translates characters to character classes
   */
  private static final char [] yycmap = yy_unpack_cmap(yycmap_packed);

  /** 
   * Translates DFA states to action switch labels.
   */
  private static final int [] YY_ACTION = yy_unpack_YY_ACTION();

  private static final String YY_ACTION_packed0 =
    "\3\0\1\1\1\2\2\3\1\4\2\5\1\6\1\7"+
    "\1\5\1\3\1\10\1\2\1\11\1\2\1\12\1\13"+
    "\3\11\1\14\1\3\1\15\1\16\1\17\1\20\1\21"+
    "\1\22\1\23\1\24\1\25\1\26\1\27\1\30\1\31"+
    "\1\32\1\5\3\33\1\34\1\33\3\1\3\0\1\35"+
    "\1\36\1\0\1\3\3\0\1\37\1\40\1\41\1\42"+
    "\1\43\1\44\1\45\1\46\1\47\1\50\1\51\1\52"+
    "\1\53\1\5\1\54\1\0\1\55\1\56\1\57\1\60"+
    "\1\61\1\62\1\1\1\63\1\1\1\64\1\35\1\0"+
    "\1\35\3\3\1\65\1\66\1\67\1\70\1\71\2\72"+
    "\1\73";

  private static int [] yy_unpack_YY_ACTION() {
    int [] result = new int[98];
    int offset = 0;
    offset = yy_unpack_YY_ACTION(YY_ACTION_packed0, offset, result);
    return result;
  }

  private static int yy_unpack_YY_ACTION(String packed, int offset, int [] result) {
    int i = 0;       /* index in packed string  */
    int j = offset;  /* index in unpacked array */
    int l = packed.length();
    while (i < l) {
      int count = packed.charAt(i++);
      int value = packed.charAt(i++);
      do result[j++] = value; while (--count > 0);
    }
    return j;
  }


  /** 
   * Translates a state to a row index in the transition table
   */
  private static final int [] yy_rowMap = yy_unpack_yy_rowMap();

  private static final String yy_rowMap_packed0 =
    "\0\0\0\62\0\144\0\226\0\310\0\372\0\310\0\u012c"+
    "\0\u015e\0\u0190\0\u01c2\0\u01f4\0\u0226\0\u0258\0\310\0\u028a"+
    "\0\u02bc\0\u02ee\0\u0320\0\u0352\0\310\0\u0384\0\u03b6\0\u03e8"+
    "\0\u041a\0\u044c\0\310\0\310\0\310\0\310\0\u047e\0\u04b0"+
    "\0\u04e2\0\310\0\310\0\310\0\310\0\u0514\0\310\0\u0546"+
    "\0\310\0\u0578\0\u05aa\0\310\0\u05dc\0\u060e\0\310\0\u0640"+
    "\0\u0672\0\u06a4\0\u01c2\0\u06d6\0\310\0\u0708\0\u073a\0\u076c"+
    "\0\u079e\0\u07d0\0\310\0\310\0\310\0\310\0\310\0\310"+
    "\0\u0802\0\310\0\310\0\u0834\0\310\0\310\0\310\0\u0866"+
    "\0\310\0\372\0\310\0\310\0\310\0\310\0\310\0\310"+
    "\0\u0898\0\310\0\u08ca\0\u08fc\0\u092e\0\u092e\0\u0708\0\u0960"+
    "\0\u0992\0\u09c4\0\u079e\0\310\0\u09f6\0\310\0\u015e\0\u041a"+
    "\0\310\0\310";

  private static int [] yy_unpack_yy_rowMap() {
    int [] result = new int[98];
    int offset = 0;
    offset = yy_unpack_yy_rowMap(yy_rowMap_packed0, offset, result);
    return result;
  }

  private static int yy_unpack_yy_rowMap(String packed, int offset, int [] result) {
    int i = 0;  /* index in packed string  */
    int j = offset;  /* index in unpacked array */
    int l = packed.length();
    while (i < l) {
      int high = ((int) packed.charAt(i++)) << 16;
      result[j++] = high | packed.charAt(i++);
    }
    return j;
  }

  /** 
   * The transition table of the DFA
   */
  private static final int [] yytrans = yy_unpack_yytrans();

  private static final String yytrans_packed0 =
    "\1\5\1\6\2\7\1\10\5\11\1\7\1\10\1\12"+
    "\1\13\1\10\1\14\1\15\1\11\1\16\1\17\1\20"+
    "\1\21\1\22\1\23\1\24\1\25\1\26\1\27\1\30"+
    "\1\31\1\32\1\33\1\34\1\35\1\36\1\37\1\40"+
    "\1\10\1\41\1\42\1\43\1\44\1\45\1\46\1\47"+
    "\1\50\4\11\1\51\1\52\26\51\1\53\32\51\1\6"+
    "\2\7\17\51\1\54\3\51\1\55\32\51\2\56\1\57"+
    "\33\56\1\60\23\56\64\0\1\7\63\0\6\10\1\0"+
    "\2\10\1\0\1\10\1\0\2\10\23\0\1\10\7\0"+
    "\5\10\4\0\6\11\1\0\2\11\1\61\1\11\1\0"+
    "\2\11\23\0\1\11\7\0\5\11\11\0\1\62\2\0"+
    "\1\12\1\63\1\62\1\0\1\12\55\0\1\64\3\0"+
    "\1\64\56\0\1\65\55\0\1\62\2\0\1\12\1\63"+
    "\1\62\1\0\1\12\1\66\40\0\1\31\1\6\2\7"+
    "\17\31\1\67\36\31\1\70\2\0\21\70\1\71\35\70"+
    "\25\0\1\25\1\0\2\72\1\0\1\26\54\0\1\25"+
    "\53\0\1\73\5\0\1\25\64\0\1\25\15\0\1\74"+
    "\30\0\1\25\61\0\1\25\7\0\1\26\51\0\1\75"+
    "\16\0\1\31\1\0\1\76\23\0\1\31\1\6\2\7"+
    "\56\31\15\0\1\77\20\0\1\100\57\0\1\101\1\0"+
    "\1\102\5\0\1\103\1\104\60\0\1\105\32\0\1\106"+
    "\74\0\1\107\35\0\6\11\1\0\2\11\1\61\1\11"+
    "\1\0\2\11\23\0\1\11\7\0\1\11\1\110\3\11"+
    "\2\0\1\51\133\0\1\111\6\0\1\112\1\7\4\0"+
    "\1\113\1\114\12\0\1\115\3\0\1\116\30\0\1\117"+
    "\1\120\2\56\1\57\33\56\1\0\23\56\2\121\1\57"+
    "\31\121\1\122\1\121\1\123\23\121\5\0\5\124\2\0"+
    "\1\124\3\0\2\124\33\0\5\124\14\0\1\125\2\0"+
    "\1\126\1\125\25\0\1\126\24\0\1\62\2\0\1\64"+
    "\1\0\1\62\1\0\1\64\45\0\1\127\1\0\1\127"+
    "\2\0\1\127\2\0\1\127\1\0\1\127\1\0\1\127"+
    "\36\0\1\127\1\0\1\127\1\130\1\131\1\7\1\132"+
    "\56\130\1\70\2\0\21\70\1\133\35\70\24\0\1\70"+
    "\62\0\1\26\100\0\1\134\51\0\1\135\7\0\1\136"+
    "\21\0\6\11\1\0\2\11\1\61\1\11\1\0\2\11"+
    "\23\0\1\11\7\0\2\11\1\137\2\11\2\121\1\57"+
    "\31\121\1\0\1\121\1\0\25\121\1\57\31\121\1\0"+
    "\1\121\1\123\23\121\4\0\6\124\1\0\2\124\1\0"+
    "\1\124\1\0\2\124\23\0\1\124\7\0\5\124\14\0"+
    "\1\125\3\0\1\125\41\0\1\31\1\6\2\7\17\31"+
    "\1\140\36\31\2\0\1\7\20\0\1\141\61\0\1\141"+
    "\102\0\1\142\15\0";

  private static int [] yy_unpack_yytrans() {
    int [] result = new int[2600];
    int offset = 0;
    offset = yy_unpack_yytrans(yytrans_packed0, offset, result);
    return result;
  }

  private static int yy_unpack_yytrans(String packed, int offset, int [] result) {
    int i = 0;       /* index in packed string  */
    int j = offset;  /* index in unpacked array */
    int l = packed.length();
    while (i < l) {
      int count = packed.charAt(i++);
      int value = packed.charAt(i++);
      value--;
      do result[j++] = value; while (--count > 0);
    }
    return j;
  }


  /* error codes */
  private static final int YY_UNKNOWN_ERROR = 0;
  private static final int YY_NO_MATCH = 1;
  private static final int YY_PUSHBACK_2BIG = 2;

  /* error messages for the codes above */
  private static final String YY_ERROR_MSG[] = {
    "Unkown internal scanner error",
    "Error: could not match input",
    "Error: pushback value was too large"
  };

  /**
   * YY_ATTRIBUTE[aState] contains the attributes of state <code>aState</code>
   */
  private static final int [] YY_ATTRIBUTE = yy_unpack_YY_ATTRIBUTE();

  private static final String YY_ATTRIBUTE_packed0 =
    "\3\0\1\1\1\11\1\1\1\11\7\1\1\11\5\1"+
    "\1\11\5\1\4\11\3\1\4\11\1\1\1\11\1\1"+
    "\1\11\2\1\1\11\2\1\1\11\1\1\3\0\1\1"+
    "\1\11\1\0\1\1\3\0\6\11\1\1\2\11\1\1"+
    "\3\11\1\1\1\11\1\0\6\11\1\1\1\11\3\1"+
    "\1\0\5\1\1\11\1\1\1\11\2\1\2\11";

  private static int [] yy_unpack_YY_ATTRIBUTE() {
    int [] result = new int[98];
    int offset = 0;
    offset = yy_unpack_YY_ATTRIBUTE(YY_ATTRIBUTE_packed0, offset, result);
    return result;
  }

  private static int yy_unpack_YY_ATTRIBUTE(String packed, int offset, int [] result) {
    int i = 0;       /* index in packed string  */
    int j = offset;  /* index in unpacked array */
    int l = packed.length();
    while (i < l) {
      int count = packed.charAt(i++);
      int value = packed.charAt(i++);
      do result[j++] = value; while (--count > 0);
    }
    return j;
  }

  /** the input device */
  private java.io.Reader yy_reader;

  /** the current state of the DFA */
  private int yy_state;

  /** the current lexical state */
  private int yy_lexical_state = YYINITIAL;

  /** this buffer contains the current text to be matched and is
      the source of the yytext() string */
  private char yy_buffer[] = new char[YY_BUFFERSIZE];

  /** the textposition at the last accepting state */
  private int yy_markedPos;

  /** the textposition at the last state to be included in yytext */
  private int yy_pushbackPos;

  /** the current text position in the buffer */
  private int yy_currentPos;

  /** startRead marks the beginning of the yytext() string in the buffer */
  private int yy_startRead;

  /** endRead marks the last character in the buffer, that has been read
      from input */
  private int yy_endRead;

  /** number of newlines encountered up to the start of the matched text */
  private int yyline;

  /** the number of characters up to the start of the matched text */
  private int yychar;

  /**
   * the number of characters from the last newline up to the start of the 
   * matched text
   */
  private int yycolumn; 

  /** 
   * yy_atBOL == true <=> the scanner is currently at the beginning of a line
   */
  private boolean yy_atBOL = true;

  /** yy_atEOF == true <=> the scanner is at the EOF */
  private boolean yy_atEOF;

  /** denotes if the user-EOF-code has already been executed */
  private boolean yy_eof_done;

  /* user code: */
	private final boolean _DEBUG = true;
	StringBuffer string = new StringBuffer();
	StringBuffer token = new StringBuffer();
	int startline, startcol;

    private Symbol symbol(int type) {
    	return symbol(type, null, yytext(), yyline, yycolumn);
    }
    private Symbol symbol(int type, Object value) {
    	return symbol(type, value, yytext(), yyline, yycolumn);
    }
    private Symbol symbol(int type, Object value, String token) {
    	return symbol(type, value, token, yyline, yycolumn);
    }
    private Symbol symbol(int type, Object value, String token, int line, int col) {
        return new MySymbol(token, type, line+1, col+1, value);
    }


  /**
   * Creates a new scanner
   * There is also a java.io.InputStream version of this constructor.
   *
   * @param   in  the java.io.Reader to read input from.
   */
  Lexer(java.io.Reader in) {
    this.yy_reader = in;
  }

  /**
   * Creates a new scanner.
   * There is also java.io.Reader version of this constructor.
   *
   * @param   in  the java.io.Inputstream to read input from.
   */
  Lexer(java.io.InputStream in) {
    this(new java.io.InputStreamReader(in));
  }

  /** 
   * Unpacks the compressed character translation table.
   *
   * @param packed   the packed character translation table
   * @return         the unpacked character translation table
   */
  private static char [] yy_unpack_cmap(String packed) {
    char [] map = new char[0x10000];
    int i = 0;  /* index in packed string  */
    int j = 0;  /* index in unpacked array */
    while (i < 136) {
      int  count = packed.charAt(i++);
      char value = packed.charAt(i++);
      do map[j++] = value; while (--count > 0);
    }
    return map;
  }


  /**
   * Refills the input buffer.
   *
   * @return      <code>false</code>, iff there was new input.
   * 
   * @exception   IOException  if any I/O-Error occurs
   */
  private boolean yy_refill() throws java.io.IOException {

    /* first: make room (if you can) */
    if (yy_startRead > 0) {
      System.arraycopy(yy_buffer, yy_startRead, 
                       yy_buffer, 0, 
                       yy_endRead-yy_startRead);

      /* translate stored positions */
      yy_endRead-= yy_startRead;
      yy_currentPos-= yy_startRead;
      yy_markedPos-= yy_startRead;
      yy_pushbackPos-= yy_startRead;
      yy_startRead = 0;
    }

    /* is the buffer big enough? */
    if (yy_currentPos >= yy_buffer.length) {
      /* if not: blow it up */
      char newBuffer[] = new char[yy_currentPos*2];
      System.arraycopy(yy_buffer, 0, newBuffer, 0, yy_buffer.length);
      yy_buffer = newBuffer;
    }

    /* finally: fill the buffer with new input */
    int numRead = yy_reader.read(yy_buffer, yy_endRead, 
                                            yy_buffer.length-yy_endRead);

    if (numRead < 0) {
      return true;
    }
    else {
      yy_endRead+= numRead;  
      return false;
    }
  }

    
  /**
   * Closes the input stream.
   */
  public final void yyclose() throws java.io.IOException {
    yy_atEOF = true;            /* indicate end of file */
    yy_endRead = yy_startRead;  /* invalidate buffer    */

    if (yy_reader != null)
      yy_reader.close();
  }


  /**
   * Resets the scanner to read from a new input stream.
   * Does not close the old reader.
   *
   * All internal variables are reset, the old input stream 
   * <b>cannot</b> be reused (internal buffer is discarded and lost).
   * Lexical state is set to <tt>YY_INITIAL</tt>.
   *
   * @param reader   the new input stream 
   */
  public final void yyreset(java.io.Reader reader) throws java.io.IOException {
    yy_reader = reader;
    yy_atBOL  = true;
    yy_atEOF  = false;
    yy_endRead = yy_startRead = 0;
    yy_currentPos = yy_markedPos = yy_pushbackPos = 0;
    yyline = yychar = yycolumn = 0;
    yy_lexical_state = YYINITIAL;
  }


  /**
   * Returns the current lexical state.
   */
  public final int yystate() {
    return yy_lexical_state;
  }


  /**
   * Enters a new lexical state
   *
   * @param newState the new lexical state
   */
  public final void yybegin(int newState) {
    yy_lexical_state = newState;
  }


  /**
   * Returns the text matched by the current regular expression.
   */
  public final String yytext() {
    return new String( yy_buffer, yy_startRead, yy_markedPos-yy_startRead );
  }


  /**
   * Returns the character at position <tt>pos</tt> from the 
   * matched text. 
   * 
   * It is equivalent to yytext().charAt(pos), but faster
   *
   * @param pos the position of the character to fetch. 
   *            A value from 0 to yylength()-1.
   *
   * @return the character at position pos
   */
  public final char yycharat(int pos) {
    return yy_buffer[yy_startRead+pos];
  }


  /**
   * Returns the length of the matched text region.
   */
  public final int yylength() {
    return yy_markedPos-yy_startRead;
  }


  /**
   * Reports an error that occured while scanning.
   *
   * In a wellformed scanner (no or only correct usage of 
   * yypushback(int) and a match-all fallback rule) this method 
   * will only be called with things that "Can't Possibly Happen".
   * If this method is called, something is seriously wrong
   * (e.g. a JFlex bug producing a faulty scanner etc.).
   *
   * Usual syntax/scanner level error handling should be done
   * in error fallback rules.
   *
   * @param   errorCode  the code of the errormessage to display
   */
  private void yy_ScanError(int errorCode) {
    String message;
    try {
      message = YY_ERROR_MSG[errorCode];
    }
    catch (ArrayIndexOutOfBoundsException e) {
      message = YY_ERROR_MSG[YY_UNKNOWN_ERROR];
    }

    throw new Error(message);
  } 


  /**
   * Pushes the specified amount of characters back into the input stream.
   *
   * They will be read again by then next call of the scanning method
   *
   * @param number  the number of characters to be read again.
   *                This number must not be greater than yylength()!
   */
  public void yypushback(int number)  {
    if ( number > yylength() )
      yy_ScanError(YY_PUSHBACK_2BIG);

    yy_markedPos -= number;
  }


  /**
   * Contains user EOF-code, which will be executed exactly once,
   * when the end of file is reached
   */
  private void yy_do_eof() throws java.io.IOException {
    if (!yy_eof_done) {
      yy_eof_done = true;
      yyclose();
    }
  }


  /**
   * Resumes scanning until the next regular expression is matched,
   * the end of input is encountered or an I/O-Error occurs.
   *
   * @return      the next token
   * @exception   IOException  if any I/O-Error occurs
   */
  public java_cup.runtime.Symbol next_token() throws java.io.IOException {
    int yy_input;
    int yy_action;

    // cached fields:
    int yy_currentPos_l;
    int yy_markedPos_l;
    int yy_endRead_l = yy_endRead;
    char [] yy_buffer_l = yy_buffer;
    char [] yycmap_l = yycmap;

    int [] yytrans_l = yytrans;
    int [] yy_rowMap_l = yy_rowMap;
    int [] yy_attr_l = YY_ATTRIBUTE;

    while (true) {
      yy_markedPos_l = yy_markedPos;

      boolean yy_r = false;
      for (yy_currentPos_l = yy_startRead; yy_currentPos_l < yy_markedPos_l;
                                                             yy_currentPos_l++) {
        switch (yy_buffer_l[yy_currentPos_l]) {
        case '\u000B':
        case '\u000C':
        case '\u0085':
        case '\u2028':
        case '\u2029':
          yyline++;
          yycolumn = 0;
          yy_r = false;
          break;
        case '\r':
          yyline++;
          yycolumn = 0;
          yy_r = true;
          break;
        case '\n':
          if (yy_r)
            yy_r = false;
          else {
            yyline++;
            yycolumn = 0;
          }
          break;
        default:
          yy_r = false;
          yycolumn++;
        }
      }

      if (yy_r) {
        // peek one character ahead if it is \n (if we have counted one line too much)
        boolean yy_peek;
        if (yy_markedPos_l < yy_endRead_l)
          yy_peek = yy_buffer_l[yy_markedPos_l] == '\n';
        else if (yy_atEOF)
          yy_peek = false;
        else {
          boolean eof = yy_refill();
          yy_markedPos_l = yy_markedPos;
          yy_buffer_l = yy_buffer;
          if (eof) 
            yy_peek = false;
          else 
            yy_peek = yy_buffer_l[yy_markedPos_l] == '\n';
        }
        if (yy_peek) yyline--;
      }
      yy_action = -1;

      yy_currentPos_l = yy_currentPos = yy_startRead = yy_markedPos_l;
  
      yy_state = yy_lexical_state;


      yy_forAction: {
        while (true) {
    
          if (yy_currentPos_l < yy_endRead_l)
            yy_input = yy_buffer_l[yy_currentPos_l++];
          else if (yy_atEOF) {
            yy_input = YYEOF;
            break yy_forAction;
          }
          else {
            // store back cached positions
            yy_currentPos  = yy_currentPos_l;
            yy_markedPos   = yy_markedPos_l;
            boolean eof = yy_refill();
            // get translated positions and possibly new buffer
            yy_currentPos_l  = yy_currentPos;
            yy_markedPos_l   = yy_markedPos;
            yy_buffer_l      = yy_buffer;
            yy_endRead_l     = yy_endRead;
            if (eof) {
              yy_input = YYEOF;
              break yy_forAction;
            }
            else {
              yy_input = yy_buffer_l[yy_currentPos_l++];
            }
          }
          int yy_next = yytrans_l[ yy_rowMap_l[yy_state] + yycmap_l[yy_input] ];
          if (yy_next == -1) break yy_forAction;
          yy_state = yy_next;

          int yy_attributes = yy_attr_l[yy_state];
          if ( (yy_attributes & 1) == 1 ) {
            yy_action = yy_state; 
            yy_markedPos_l = yy_currentPos_l; 
            if ( (yy_attributes & 8) == 8 ) break yy_forAction;
          }

        }
      }

      // store back cached position
      yy_markedPos = yy_markedPos_l;

      switch (yy_action < 0 ? yy_action : YY_ACTION[yy_action]) {
        case 21: 
          { return symbol(sym.GUARD); }
        case 60: break;
        case 58: 
          { return symbol(sym.CHAR_CODE_LITERAL,	yytext()); }
        case 61: break;
        case 42: 
          { return symbol(sym.MINUS_DOT); }
        case 62: break;
        case 40: 
          { return symbol(sym.RBRACE_UNDERBAR); }
        case 63: break;
        case 16: 
          { return symbol(sym.RPAREN); }
        case 64: break;
        case 49: 
          { token.append(yytext()); string.append("\n"); }
        case 65: break;
        case 30: 
          { return symbol(sym.PLUS_DOT); }
        case 66: break;
        case 31: 
          { return symbol(sym.NEGATIVE); }
        case 67: break;
        case 10: 
          { return symbol(sym.BACKSLASH); }
        case 68: break;
        case 22: 
          { return symbol(sym.HAT); }
        case 69: break;
        case 17: 
          { return symbol(sym.LBRACE); }
        case 70: break;
        case 43: 
          { string.setLength(0); token.setLength(0); token.append(yytext()); startline = yyline; startcol = yycolumn; yybegin(QUOTED); }
        case 71: break;
        case 25: 
          { return symbol(sym.LBRACKET); }
        case 72: break;
        case 54: 
          { return symbol(sym.RBRACE_SLASH_AT); }
        case 73: break;
        case 24: 
          { return symbol(sym.PROCVAR); }
        case 74: break;
        case 8: 
          { string.setLength(0); token.setLength(0); token.append(yytext()); startline = yyline; startcol = yycolumn; yybegin(STRING); }
        case 75: break;
        case 44: 
          { token.append(yytext()); yybegin(YYINITIAL); return symbol(sym.QUOTED_STRING, string.toString(), token.toString(), startline, startcol); }
        case 76: break;
        case 39: 
          { return symbol(sym.RBRACE_AT); }
        case 77: break;
        case 53: 
          { return symbol(sym.SYMBOL_NAME,		yytext()); }
        case 78: break;
        case 12: 
          { return symbol(sym.SLASH); }
        case 79: break;
        case 51: 
          { yybegin(YYINITIAL); }
        case 80: break;
        case 19: 
          { return symbol(sym.RULEVAR); }
        case 81: break;
        case 7: 
          { return symbol(sym.PLUS); }
        case 82: break;
        case 57: 
          { return symbol(sym.MOD); }
        case 83: break;
        case 6: 
          { return symbol(sym.PERIOD); }
        case 84: break;
        case 37: 
          { return symbol(sym.RBRACE_SLASH); }
        case 85: break;
        case 59: 
          { return symbol(sym.RBRACE_UNDERBAR_SLASH_AT); }
        case 86: break;
        case 13: 
          { return symbol(sym.ASTERISK); }
        case 87: break;
        case 52: 
          { return symbol(sym.PATHED_ATOM_NAME,	yytext()); }
        case 88: break;
        case 34: 
          { yybegin(COMMENT); }
        case 89: break;
        case 4: 
          { return symbol(sym.LINK_NAME,			yytext(), yytext(), yyline, yycolumn); }
        case 90: break;
        case 26: 
          { return symbol(sym.RBRACKET); }
        case 91: break;
        case 11: 
          { return symbol(sym.COLON); }
        case 92: break;
        case 46: 
          { token.append(yytext()); string.append("\t"); }
        case 93: break;
        case 35: 
          { return symbol(sym.ASTERISK_DOT); }
        case 94: break;
        case 36: 
          { return symbol(sym.ASTERISK_ASTERISK); }
        case 95: break;
        case 33: 
          { return symbol(sym.SLASH_DOT); }
        case 96: break;
        case 38: 
          { return symbol(sym.RBRACE_ASTERISK); }
        case 97: break;
        case 15: 
          { return symbol(sym.LPAREN); }
        case 98: break;
        case 18: 
          { return symbol(sym.RBRACE); }
        case 99: break;
        case 29: 
          { return symbol(sym.NUMBER_NAME,		yytext()); }
        case 100: break;
        case 48: 
          { token.append(yytext()); string.append("\\"); }
        case 101: break;
        case 9: 
          { return symbol(sym.RELOP, yytext()); }
        case 102: break;
        case 3: 
          { /* just skip */ }
        case 103: break;
        case 45: 
          { token.append(yytext()); string.append("\r"); }
        case 104: break;
        case 55: 
          { return symbol(sym.RBRACE_UNDERBAR_SLASH); }
        case 105: break;
        case 20: 
          { return symbol(sym.MINUS); }
        case 106: break;
        case 27: 
          { token.append(yytext()); string.append( yytext() ); }
        case 107: break;
        case 50: 
          { token.append(yytext()); string.append("\f"); }
        case 108: break;
        case 32: 
          { return symbol(sym.RULE, new Integer(yyline+1)); }
        case 109: break;
        case 2: 
          { throw new Error("Illegal character <"+yytext()+"> at line "+(yyline+1)); }
        case 110: break;
        case 28: 
          { token.append(yytext()); yybegin(YYINITIAL); return symbol(sym.STRING, string.toString(), token.toString(), startline, startcol); }
        case 111: break;
        case 41: 
          { return symbol(sym.RULENAMESEP); }
        case 112: break;
        case 56: 
          { return symbol(sym.RBRACE_UNDERBAR_AT); }
        case 113: break;
        case 47: 
          { token.append(yytext()); string.append("\""); }
        case 114: break;
        case 5: 
          { return symbol(sym.ATOM_NAME,			yytext()); }
        case 115: break;
        case 14: 
          { return symbol(sym.COMMA); }
        case 116: break;
        case 23: 
          { return symbol(sym.TILDE); }
        case 117: break;
        case 1: 
          {  }
        case 118: break;
        default: 
          if (yy_input == YYEOF && yy_startRead == yy_currentPos) {
            yy_atEOF = true;
            yy_do_eof();
            switch (yy_lexical_state) {
            case STRING:
              { throw new Error("EOF in string"); }
            case 99: break;
            case QUOTED:
              { throw new Error("EOF in quoted name"); }
            case 100: break;
            case COMMENT:
              { throw new Error("EOF in comment"); }
            case 101: break;
            default:
              { return new java_cup.runtime.Symbol(sym.EOF); }
            }
          } 
          else {
            yy_ScanError(YY_NO_MATCH);
          }
      }
    }
  }


}
