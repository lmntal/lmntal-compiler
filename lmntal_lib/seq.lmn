//seq.run(Data,[F1,F2...]).
//DataにF1,F2...を順番に適用する
{module(seq). 
 seq.run(Data,[A|RES]),{+A. @p},{+Data. $p. @r}/
  :- {+Data. $p. @p},seq.run(Data,RES).
 seq.run(Data,[]),{+Data. @p. $p.} :- $p.
}
